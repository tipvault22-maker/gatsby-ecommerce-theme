name: Uptime Ping
on:
  schedule: [ { cron: "*/15 * * * *" } ]   # every 15 min
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: curl -sS -o /dev/null -w "%{http_code}\n" https://tipvault22.com
